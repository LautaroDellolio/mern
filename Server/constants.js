const DB_USER = "admin";
const DB_PASSWORD = "admin123";
const DB_HOST = "cluster0.oequs.mongodb.net";

const JWT_SECRET_KEY = "bocaComoTeDueleLaCola"

const API_VERSION = "v1"
const IP_SERVER = "localhost"

module.exports = {
    DB_USER,
    DB_PASSWORD,
    DB_HOST,
    API_VERSION,
    IP_SERVER,
    JWT_SECRET_KEY
}